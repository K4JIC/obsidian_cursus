
mallocのメモリ確保失敗時のエラーを上位関数に運搬するのが大変だった
- エラー時の処理は関数の先頭に書く。
- 自作関数を呼び出した際は、直後にエラー時の処理を書く。
- mallocを利用する関数は名前を変えた方がいいかも。

mallocは一関数に一個

上位の関数から書く

エラー処理
- 配列のポインタを引数にとる場合
  ```c
  char *funcname(char **s)
  {
	  if (!s || !*s)
		  return (NULL);
  }
  ```
  のように、中身の有無もチェックする！
