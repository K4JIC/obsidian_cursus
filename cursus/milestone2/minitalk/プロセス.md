
```shell
kill -SIGTERM -1
```
ですべてのプロセスを終了。
PID=-1 は存在しないが、killの機能としてPIDに-1を指定するとすべてのプロセスを終了させる（man kill）

```shell
kill -SIGTERM LONG_MAX(64bit)
```
これもすべてのプロセスを終了。
bashではlong max以上の数字を扱えずオーバーフローするので、`kill -SIGTERM -1` と同じことが起きる。